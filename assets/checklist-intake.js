(function(){const form=document.querySelector('form[name="ai-readiness-checklist"]');if(!form)return;function save(){const audience=form.querySelector('[name="audience_type"]')?.value||'',goal=form.querySelector('[name="message"]')?.value||'';try{sessionStorage.setItem('aik_checklist_context',JSON.stringify({audience,goal:goal.slice(0,300)}));}catch{}}form.addEventListener('change',save);form.addEventListener('input',save);form.addEventListener('submit',save);})();
